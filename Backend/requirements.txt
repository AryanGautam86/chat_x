fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.20          # multipart/form-data parsing for Form(...)
python-dotenv==1.1.1
sqlalchemy==2.0.36

PyJWT==2.10.1                     # access tokens
bcrypt==4.2.1                     # password hashing
email-validator==2.2.0            # pydantic EmailStr
psycopg2-binary==2.9.10           # only used when DATABASE_URL points at Postgres
# Google ID-token verification uses google-auth, already pulled in by
# langchain-google-genai.

langchain==1.0.0
langchain-community==0.4.0        # FAISS vectorstore integration
langchain-google-genai==2.1.12    # Gemini chat + embeddings
faiss-cpu==1.8.0.post1
numpy==1.26.4                     # required by faiss-cpu
