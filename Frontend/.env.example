# Copy to Frontend/.env for local development.
# In production the API is served from the same origin as the bundle, so this
# is left unset and the client uses a relative baseURL.

VITE_API_URL=http://127.0.0.1:8000
